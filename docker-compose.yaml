version: '3.6'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/var/www/html
    ports:
      - "7000:8000"
    extra_hosts:
      - 'host.docker.internal:host-gateway'

  prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    ports:
      - "7001:8000"
    extra_hosts:
      - 'host.docker.internal:host-gateway'
